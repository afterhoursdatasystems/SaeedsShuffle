# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1

# This section tells App Hosting to inject the secret value into the
# specified environment variable.
secrets:
  - secret: ss_secret
    env: FIREBASE_SERVICE_ACCOUNT_JSON
      
# The following environment variables are available during the build process
# and are sourced from your Firebase project's configuration. You do not
# need to define them here for them to be available at runtime.
# NEXT_PUBLIC_FIREBASE_API_KEY
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
# NEXT_PUBLIC_FIREBASE_DATABASE_URL
# NEXT_PUBLIC_FIREBASE_PROJECT_ID
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
# NEXT_PUBLIC_FIREBASE_APP_ID
